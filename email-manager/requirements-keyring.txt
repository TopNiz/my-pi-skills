# Native credential-store support for Gmail OAuth2.
# macOS uses Python keyring with the Keychain backend.
# Fedora/Linux uses the installed Secret Service CLI (secret-tool).
keyring>=25.0; sys_platform == "darwin"
