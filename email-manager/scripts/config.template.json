{
  "_comment": "Email Manager configuration template. Copy to config.json and edit. config.json is gitignored and holds no passwords: Gmail access uses OAuth2 (scripts/auth.py) with the refresh token in the native OS credential store.",

  "filters": {
    "max_emails": 50,
    "fetch_days_back": 7,
    "include_seen": false,
    "folders": ["INBOX"]
  },

  "invoices": {
    "storage_dir": "~/Invoices",
    "auto_extract": true,
    "save_attachments": true
  },

  "categories": {
    "taxonomy_file": "../references/CATEGORIES.md"
  },

  "protected_senders": {
    "description": "Emails from these senders will NEVER be deleted or unsubscribed.",
    "list": []
  },

  "routing": {
    "description": "Gmail label routing rules. Format: {'sender': 'user@domain.com' | '*@domain.com', 'filter': 'subject:invoice', 'label': 'Folder/Label'}",
    "rules": []
  }
}
