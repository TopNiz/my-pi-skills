[project]
name = "pi-google-calendar"
version = "1.0.0"
description = "Google Calendar OAuth2 + API access for the pi google-calendar skill"
requires-python = ">=3.10"
dependencies = [
    "google-auth>=2.0",
    "google-auth-oauthlib>=1.0",
    "google-api-python-client>=2.0",
]

# Scripts only: nothing to build or install, dependencies only.
[tool.uv]
package = false
